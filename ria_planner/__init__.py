"""RIA Planning Agent — a financial-plan-drafting assistant for advisors.

Two layers:
  * engine.py  — deterministic retirement math (no AI, no API key needed)
  * agent.py   — Claude writes the advisor-facing plan from that math
"""

__version__ = "0.1.0"
